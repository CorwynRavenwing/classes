class Solution:
    def assignEdgeWeights(self, edges: List[List[int]], queries: List[List[int]]) -> List[int]:
        
        # NOTE: do the other version first
        # the difference is, that is {root} to {farthest}
        # and this is {list of [{node 1} to {node 2}]}
        pass

# NOTE: Acceptance Rate 59.8% (HARD)
