class Solution:
    def totalWaviness(self, num1: int, num2: int) -> int:
        
        '''
        NOTE: should calculate wavinessBelow(n) and subtract
        num1 from num2 for the answer.

        should create a new function:
        waviness(length, first_digit, direction)
        which is calculated as the sum of
        1 + W(len-1, every digit in [direction] from first_digit, - direction)
        plus all the possible W(len-1, every digit in the wrong direction)
        '''

        pass

# NOTE: Acceptance Rate 82.0% (medium)

# NOTE: Incomplete, I haven't finished the algorithm yet
